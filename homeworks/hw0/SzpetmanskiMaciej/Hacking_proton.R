install.packages("proton")
library(proton)
proton()

1:n

n<-nrow(employees)
for(i in 1:n){
  if(i == )
}
employees$surname == "Insecure"  -> temp
temp

proton(action = "login", login="johnins")
password <- top1000passwords
password
n<-length(password)
for(i in 1:n){
  
  if(proton(action = "login", login = "johnins", password=password[i])=="Success! User is logged in!"){
    print(password[i])
  }
}
proton(action = "login", login = "johnins", 123456)

proton(action = "server", host = "johnins")


n<-nrow(employees)
for(i in 1:n){
  if(employees[])
}